module example;

export package Circle {
    internal static f64 pi = 3.1416;
    internal i64 r = 25;

    export f64 getArea() {
        return pi * (r * r);
    }
    export f64 getCircumference () {
        return 2 * pi * r;
    }
}
